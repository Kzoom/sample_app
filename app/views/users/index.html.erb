<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<!-- since 'users', Rails will look for partial _user.html.erb  -->
	<!-- since '@users', Rails will iterate over retrieved set  -->
	<%= render @users %>  
</ul>

<%= will_paginate %>